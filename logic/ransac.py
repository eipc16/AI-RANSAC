import numpy as np

class Ransac:
    def start(pairs, max_error, iterations, transformation, dest_path):
        result = self._run(pairs, max_error, iterations, transformation)

    def _run(pairs, max_error, iterations, transformation):
        pass

    def _get_best_model():
        pass

    @staticmethod
    def _model_error(model, data):
        x, y = data[0]['x'], data[1]['y']
        pass
